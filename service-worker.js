self.addEventListener("install", (event) => { console.log("Service worker installed"); self.skipWaiting(); }); self.addEventListener("activate", (event) => { console.log("Service worker activated"); event.waitUntil( caches.open("radio-cache").then((cache) => { return cache.addAll([ "/notre-radio-site/", "/notre-radio-site/index.html", "/notre-radio-site/manifest.json", "/notre-radio-site/icon-192.png", "/notre-radio-site/icon-512.png" ]); }) ); }); self.addEventListener("fetch", (event) => { event.respondWith( caches.match(event.request).then((response) => { return response || fetch(event.request); }) ); });
